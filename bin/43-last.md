🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-00. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.

가끔 한국어로 물어도 영어로 답변이 나오면 불편하니 소스가 아닌 답변은 모두 한국어로 해줘.

   3,.   w   00a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-00.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-01. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   01a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-01.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-02. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   02a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-02.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-03. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   03a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-03.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-04. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   04a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-04.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-05. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   05a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-05.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-06. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   06a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-06.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-07. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   07a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-07.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-08. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   08a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-08.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-09. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   09a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-09.


🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-10. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.


   3,.   w   10a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-10.





#--- |||| (여기부터 34 줄을 복사한뒤, bash 창에 붙여 실행한다.)
#--- ||||
#--- vvvv
cd /home/proenpi/archive/myusb/gemcli-2510/test/gemcli1028.1706-window용_pomodoro앱_작성; use_for="window용 pomodoro앱 작성"; backup_dir="../last-gemcli1028.1706"
msg00="   파일의 내용을 읽고 지시에 따라줘.   "
msg01="   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   "
end_no="110"
new_no="111"
echo "#---->"
echo "#----> 그냥 Enter 만 눌러서 (${new_no}) 번 으로 지정하거나, 직접 시작번호를 입력하세요."
read begin_no
if [ "x100" = "x" ]; then
    begin_no=${new_no}
fi
for (( i=${begin_no}; i<=${end_no}; i++ ))
do
    cat >> ../last-gemcli1028.1706-99-window용_pomodoro앱_작성.md <<__SRC_EOF__

🔥 ( window용 pomodoro앱 작성 )
### 🔥 
gemcli1028.1706-${i:1}. ---- 아래 3 의 값을 이 줄의 번호로 바꾸고, 이 줄은 제목으로 다시쓴다.

   3,.   w   ${i:1}a-gemcli1028.1706.txt   파일의 내용을 읽고 지시에 따라줘.      #--
   ----------
mkdir ~/Downloads/gemcli-2025 ; rsync -avzr -e 'ssh -p 5822' proenpi@pi:a*/m*/g*/g*.1706-*             ~/Downloads/gemcli-2025/;  cd ~/Downloads/gemcli-2025/g*.1706-*/ ; ll #--   만들어진 txt 를 geminicli 가 있는 로컬로 복사하는 스크립트.   

🔋
### 🔋 gemcli1028.1706-${i:1}.

__SRC_EOF__
done
#--- ^^^^
#--- ||||
#--- |||| qna 를 10개 더 만든다.  🔥 (    window용 pomodoro앱 작성    )
 
